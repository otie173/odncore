package world

func generateBarrier() {
	for x := -WORLD_SIZE / 2; x <= WORLD_SIZE/2; x++ {
		// generate x barrier
	}

	for y := -WORLD_SIZE / 2; y <= WORLD_SIZE/2; y++ {
		// generate y barrier
	}
}
